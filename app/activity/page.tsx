"use client";import Link from "next/link";export default function Activity(){const events=[["POLICY CHECK","1 USDC","ALLOWED"],["POLICY CHECK","6 USDC","BLOCKED"],["EMERGENCY STOP","1 USDC","BLOCKED"]];return <main className="market"><nav className="topnav"><Link href="/" className="brand">XAVERUS<span>ACTIVITY</span></Link><div className="navlinks"><a href="/">Marketplace</a><a href="/agents">My Agents</a><a href="/safety">Safety</a><a className="active" href="/activity">Activity</a></div></nav><section className="section"><div className="eyebrow">PROOF-OF-ACTION READY</div><h1>Agent <em>Activity.</em></h1><p>Policy decisions are represented separately from transaction execution.</p><div className="agentCard" style={{maxWidth:800}}>{events.map((e,i)=><div className="event" key={i}><div><b>{e[0]}</b><span>{e[1]} · X Layer · server-side decision</span></div><strong className={e[2]==="ALLOWED"?"good":"bad"}>{e[2]}</strong></div>)}</div></section></main>}